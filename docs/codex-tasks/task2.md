You are implementing Task 2 of the project in the current directory (Python 3.13 venv at `.venv`, FastAPI + LangGraph). Read README.md, docs/architecture.md (section "도구 목록"), app/config.py and app/schemas/plan.py first. Do NOT modify those files, specs/, docs/architecture.md, or .env. Do not commit. Do not print API keys. Assume NO network access: tests must not call real APIs.

## Goal of Task 2
Implement the four agent tools as pure Python functions wrapped as LangChain tools, with mocked-transport tests. Keys for NAVER/Tavily are NOT yet in .env — the code must handle missing keys gracefully (return an error dict, never raise or crash the agent).

## Package layout (create exactly these)
- `app/tools/__init__.py` — exports `ALL_TOOLS = [web_search, geocode_place, get_route, get_weather]` (the LangChain tool objects).
- `app/tools/http.py` — `get_client(transport=None) -> httpx.Client` with `timeout=10`. Every tool module calls this so tests can inject `httpx.MockTransport`. Module-level `_client_factory` that tests can monkeypatch, or accept an optional `client` kwarg in the plain functions — pick one, use it consistently.
- `app/tools/weather.py` — `fetch_weather(lat: float, lng: float, date: str, client=None) -> dict`. Open-Meteo: `GET https://api.open-meteo.com/v1/forecast` with `latitude, longitude, daily=weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max, timezone=Asia/Seoul, start_date=date, end_date=date`. Map WMO weather_code to a Korean summary (0 맑음, 1-3 구름 조금/많음, 45-48 안개, 51-67 비, 71-77 눈, 80-82 소나기, 95-99 뇌우). Return `{"summary": str, "temp_min": float, "temp_max": float, "precipitation_probability": float}` — this must match the `Weather` model in app/schemas/plan.py. If date is outside the forecast range (API returns error or empty arrays) return `{"error": "..."}`.
- `app/tools/geocode.py` — `fetch_geocode(name: str, region: str = "", client=None) -> dict`. NAVER Cloud Platform Maps Geocoding: `GET https://maps.apigw.ntruss.com/map-geocode/v2/geocode?query=<region + ' ' + name>` headers `x-ncp-apigw-api-key-id` / `x-ncp-apigw-api-key` from settings.NAVER_MAP_CLIENT_ID / NAVER_MAP_CLIENT_SECRET. Take the first `addresses[]` item: `{"lat": float(y), "lng": float(x), "address": roadAddress or jibunAddress}`. Empty result → `{"error": "not_found", "query": ...}`. Missing keys → `{"error": "missing_api_key"}` without making a request.
- `app/tools/route.py` — `fetch_route(from_lat, from_lng, to_lat, to_lng, mode: str = "car", client=None) -> dict` returning `{"mode": mode, "distance_km": float, "duration_min": int}` (matches `TravelLeg`). `car`: NCP Directions 5 `GET https://maps.apigw.ntruss.com/map-direction/v1/driving?start=<lng>,<lat>&goal=<lng>,<lat>&option=trafast`, same headers; parse `route.trafast[0].summary.distance` (m) and `.duration` (ms). `walk` and `transit`: no API call — haversine distance × 1.3 road factor, walk speed 4 km/h, transit 20 km/h + 10 min wait. If car API fails or keys missing, fall back to the haversine estimate with 40 km/h and add `"estimated": true`.
- `app/tools/search.py` — `fetch_web_search(query: str, max_results: int = 5, client=None) -> list[dict]`. Tavily `POST https://api.tavily.com/search` json `{"api_key": settings.TAVILY_API_KEY, "query": query, "max_results": max_results, "search_depth": "basic"}`. Return `[{"title", "snippet" (= content truncated to 300 chars), "url"}]`. Missing key → `[{"error": "missing_api_key"}]`.
- In each module, below the plain function, define the LangChain tool with `@tool` from `langchain_core.tools`, named exactly `web_search`, `geocode_place`, `get_route`, `get_weather`, with a Korean docstring the LLM will read (what it does, when to use it, what it returns). The tool just calls the plain function and returns the dict/list (LangChain serializes it).
- Put the haversine helper in `app/tools/geo.py`.

## Tests — `tests/test_tools.py`
Use `httpx.MockTransport` with handler functions that assert the request URL/headers/params and return canned JSON. Cover: weather happy path + WMO mapping + out-of-range error; geocode happy path + empty result + missing key (monkeypatch settings attributes to ""); route car happy path (distance/duration unit conversion), walk/transit haversine (Daejeon cathedral 36.3258,127.4243 → Sungsimdang 36.3276,127.4273 should be ~0.3-0.4 km walk), car fallback when API returns 500; search happy path + missing key. Also a test that `ALL_TOOLS` has 4 tools with the exact names and that each tool's `args_schema` has the expected fields. Run `.venv/Scripts/python -m pytest -q` and make everything pass (existing 6 schema tests must still pass).

## Report
Write `docs/task2-tools.md` in Korean: file list, each tool's input/output contract, how missing keys are handled, the full `pytest -q` output as text. End your reply with a short summary of what you did and anything uncertain (e.g. exact NCP response field names you were unsure about).
