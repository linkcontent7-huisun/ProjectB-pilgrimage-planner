You are implementing Task 1 of the project in the current directory (a fresh git repo, Python FastAPI + LangGraph). Read README.md, docs/architecture.md, specs/requirements-checklist.md and specs/plan-schema.json first — they define the design. Do NOT change those docs except where explicitly told below.

## Goal of Task 1
Set up the Python project skeleton and PROVE that LangGraph tool-calling works against the Codyssey LLM API (OpenAI-compatible endpoint given in .env as OPENAI_BASE_URL / OPENAI_API_KEY / OPENAI_MODEL). This is an experiment gate: the result decides whether we use LangChain's ChatOpenAI or fall back to the raw `openai` SDK inside LangGraph nodes.

## Steps
1. Create `requirements.txt` with pinned, current versions: fastapi, uvicorn[standard], sse-starlette, pydantic>=2, python-dotenv, httpx, langgraph, langchain-core, langchain-openai, openai, jsonschema, pytest, pytest-asyncio. Create a venv at `.venv` and install. Python here is 3.14 — if any package fails on 3.14, note it in the report and pin a version that works.
2. Create `app/config.py` (mirror the style: a `Settings` class reading env via python-dotenv; fields OPENAI_API_KEY, OPENAI_BASE_URL, OPENAI_MODEL, NAVER_MAP_CLIENT_ID, NAVER_MAP_CLIENT_SECRET, TAVILY_API_KEY, ALLOWED_ORIGINS list).
3. Create `app/schemas/plan.py`: Pydantic v2 models that are EXACTLY equivalent to specs/plan-schema.json (PilgrimagePlan, Day, Place, Coordinates, Weather, TravelLeg; enums for persona and place type; `extra="forbid"`). Add `tests/test_plan_schema.py` that validates a good sample and rejects 3 bad samples (missing field, out-of-range lat, bad time format), and a test that the Pydantic JSON schema accepts the same sample that `jsonschema` validates against specs/plan-schema.json.
4. Create `scripts/probe_tool_calling.py`: a minimal LangGraph StateGraph with one LLM node bound to ONE dummy tool `get_weather(city: str) -> str` (returns a hardcoded string) and a ToolNode, using `langchain_openai.ChatOpenAI(base_url=settings.OPENAI_BASE_URL, api_key=..., model=settings.OPENAI_MODEL)`. Ask it "대전의 내일 날씨를 알려줘" and print every message in the final state. Run it. Known Codyssey API gotchas (from a sibling project): `response_format` is unsupported (HTTP 400), and unknown extra fields in messages sent back to the API cause 400. If ChatOpenAI fails with 400, capture the exact error body, then try: (a) `model_kwargs`/`disabled_params` to strip fields, (b) if still failing, write `scripts/probe_tool_calling_raw.py` that uses the raw `openai` SDK client inside a LangGraph node with a hand-written tool loop (role/content/tool_calls only when echoing the assistant message). Run it too. Keep both scripts.
5. Write the results to `docs/task1-tool-calling-probe.md` in Korean: environment (python version, key package versions from pip freeze), exact command run, full terminal output (as text, not screenshot), which approach worked, and a one-paragraph recommendation. Then create `docs/environment.md` in Korean with `python --version` and `pip freeze` output as text.
6. Do NOT commit. Do NOT touch .env. Do NOT print the API key anywhere. Do not create a frontend yet.

## Report
End with a short summary: what works, what failed (with error text), files created, and your recommendation (ChatOpenAI vs raw SDK).
