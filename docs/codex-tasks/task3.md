You are implementing Task 3 of the project in the current directory (Python 3.13 venv at `.venv`, LangGraph). Read README.md, docs/architecture.md, specs/plan-schema.json, app/schemas/plan.py, app/tools/*.py, data/holy_sites.json (structure only) and tests/test_tools.py first. Do NOT modify specs/, docs/architecture.md, app/tools/, app/schemas/, data/, or .env. Do not commit. Assume NO network: tests must not call the real LLM or APIs. Existing 15 tests must keep passing.

## Goal of Task 3
Build the LangGraph planning agent: `collect -> research (tool loop) -> compose -> validate -> (retry compose) -> finalize`, plus a revise flow. It streams progress/log events through a callback so Task 4 can forward them over SSE. The LLM is `langchain_openai.ChatOpenAI` (already proven to work with tool calling; see docs/task1-tool-calling-probe.md). Known API gotcha: `response_format` is NOT supported - ask for JSON in the prompt and strip ```json fences before parsing.

## Files to create
- `app/agent/__init__.py`
- `app/agent/llm.py` - `get_llm(temperature=0.3) -> ChatOpenAI` built from settings (base_url, api_key, model). Everything else must obtain the LLM only through this function so tests can monkeypatch it.
- `app/agent/events.py` - `Emit = Callable[[dict], None]`; helpers `status(stage, message)`, `log_thinking(msg)`, `log_tool_call(tool, args)`, `log_tool_output(tool, summary)`, `log_decision(msg)`, `result(plan)`, `error(msg)` each returning a dict `{"event": "status"|"log"|"result"|"error", "data": {...}}` exactly matching the "SSE 이벤트 형식" section of docs/architecture.md (log data has `kind` in thinking|tool_call|tool_output|decision).
- `app/agent/personas.py` - `PERSONAS: dict[str, dict]` for `mass_centered`, `walking`, `frugal` with keys `label` (Korean), `rules` (Korean bullet text injected into prompts), `default_mode` (car / walk / transit), `max_places_per_day`, `daily_walk_km_limit` (walking: 12, others: None). mass_centered: every day must contain one `mass` or `holy_site` place with `mass_time`, and the schedule must be built around that time; walking: sites clustered within walking range, include a cafe/rest, travel mode walk; frugal: estimated_cost_per_person <= budget, prefer transit, cheap meals, note savings in `reason`.
- `app/agent/candidates.py` - `load_sites() -> list[dict]` (cached read of data/holy_sites.json) and `select_candidates(region: str, persona: str, limit: int = 12) -> list[dict]`: match `site["city"] == region` or region contained in `site["address"]`; if fewer than 4 matches, expand by haversine distance (`app.tools.geo.haversine_km`) from the centroid of the matches (or from 대흥동 주교좌성당 if no matches) - radius 15 km for `walking`, 70 km otherwise - sorted by distance. `plenary_indulgence` sites first within equal distance buckets. Return dicts with id, name, city, address, lat, lng, category, plenary_indulgence, website.
- `app/agent/state.py` - `PlanRequest` Pydantic model: `region: str`, `start_date: str (YYYY-MM-DD)`, `nights: int >= 0`, `party_size: int >= 1`, `budget_per_person: int >= 0`, `persona: Persona` (from app.schemas.plan), `preferences: str = ""` (free text). `AgentState(TypedDict, total=False)`: request (dict), candidates, messages (Annotated[list, add_messages]), research_notes: str, draft: str, plan: dict, validation_errors: list[str], attempts: int, revise_request: str | None, previous_plan: dict | None, tool_call_cap: int.
- `app/agent/prompts.py` - Korean prompt builders: `research_system_prompt(request, persona, candidates)` tells the LLM it is a pilgrimage research assistant with tools; it must (1) call get_weather once per travel day at the centroid, (2) call web_search for mass times of each candidate site it intends to use (query pattern `"<성지명> 미사 시간"`), (3) call web_search / geocode_place for restaurants, cafes, lodging near the sites as needed, (4) call get_route between consecutive sites for the chosen mode, (5) finish with a plain-text Korean summary of findings (no JSON) starting with `조사 요약:`. Hard cap: at most 12 tool calls; say so in the prompt. `compose_system_prompt(persona)` and `compose_user_prompt(request, candidates, research_notes, previous_plan, revise_request, validation_errors)` produce the plan JSON: embed the required field list and enums (derived from specs/plan-schema.json at import time - load the file and render required/enum info, do not hand-copy), days must equal nights+1 with consecutive dates from start_date, times HH:MM, coordinates only from geocode/candidate data (never invent), every place has `reason`, output ONLY a JSON object. When validation_errors are given, list them and say "이 오류를 고쳐 다시 출력하라". When previous_plan + revise_request are given, say to keep everything not affected and recompute order/times/travel for the rest.
- `app/agent/nodes.py` - node functions, each `(state, config) -> dict`, reading `emit = config["configurable"].get("emit") or (lambda e: None)`:
  - `collect`: validate request via PlanRequest, select candidates, emit status("collect", ...) and a thinking log listing candidate names.
  - `research`: emit status("research", "성지·미사 시간·날씨 조사 중"). Run a manual tool loop (not the prebuilt ToolNode, so we can emit per call): llm = get_llm().bind_tools(ALL_TOOLS); loop up to `tool_call_cap` (default 12) tool calls: invoke, if AIMessage has tool_calls -> for each call emit log_tool_call, execute the matching tool from ALL_TOOLS by name via `tool.invoke(args)`, emit log_tool_output with a <=200-char summary (json.dumps ensure_ascii=False truncated), append ToolMessage; if the AIMessage has text content emit log_thinking. Stop when no tool_calls. If the cap is hit, send one final HumanMessage "도구 사용 한도에 도달했다. 지금까지의 조사 내용을 '조사 요약:'으로 정리하라." and take that answer. Store the final text in research_notes. If revise_request is set, prepend it to the human message so the research is focused on the change, and prepend the previous research_notes as known facts.
  - `compose`: emit status("compose", "일정 초안 작성 중"), call get_llm(temperature=0.2) with compose prompts, strip fences (helper `strip_json_fences(text)` handling ```json ... ```, ``` ... ``` and bare JSON), store `draft`. emit log_decision with a one-line summary (plan_name if parseable).
  - `validate`: parse JSON (json.JSONDecodeError -> error "JSON 파싱 실패: ..."), `PilgrimagePlan.model_validate` (collect pydantic errors as `loc: msg` strings, max 10), then logical checks in `app/agent/validators.py`: `logical_errors(plan: PilgrimagePlan, request: PlanRequest) -> list[str]` - days count == nights+1; dates consecutive from start_date; within each day, `order` is 1..n and start_time < end_time and each start >= previous end; for mass_centered persona each day has a place with `mass_time`; for walking persona total walk distance per day (sum of travel_from_previous.distance_km where mode == walk, or haversine between consecutive coordinates when missing) <= daily_walk_km_limit; estimated_cost_per_person (or sum of estimated_cost) <= budget_per_person x 1.2 (error string starting with "예산 초과"). On errors: store validation_errors, attempts += 1, emit log_decision("검증 실패 (N/3): ..."). On success: store `plan` (model_dump(exclude_none=True)), clear errors.
  - `finalize`: emit status("finalize", "이동 시간 계산 중"). For each consecutive pair of places within a day where `travel_from_previous` is missing, call `fetch_route` with the persona default_mode and fill it. Recompute `estimated_cost_per_person` = sum of place.estimated_cost if any place has a cost. Re-validate with PilgrimagePlan and emit result(plan).
- `app/agent/graph.py` - `build_graph()` compiles: collect -> research -> compose -> validate -> conditional: ok -> finalize -> END; errors and attempts < 3 -> compose; errors and attempts >= 3 -> END (state keeps validation_errors). Also `class PlanGenerationError(Exception)` with `.errors`. Public API:
  - `run_plan(request: dict, emit: Emit | None = None) -> dict` - invokes the graph with `config={"configurable": {"emit": emit}}`, returns `{"plan": plan_dict, "research_notes": str}`; raises PlanGenerationError (and emits error event) if no plan.
  - `run_revise(previous_plan: dict, request: dict, revise_request: str, research_notes: str, emit=None) -> dict` - same graph but initial state has previous_plan, revise_request, the prior research_notes and tool_call_cap=6.
- `scripts/run_agent.py` - CLI: `python scripts/run_agent.py --region 대전 --start 2026-09-20 --nights 0 --party 2 --budget 50000 --persona mass_centered` prints every event as one line (`[status] ...`, `[Tool Call] ...`, `[Tool Output] ...`, `[Thinking] ...`, `[Decision] ...`) and finally the plan JSON pretty-printed (ensure_ascii=False). Do not run it (no network) - I will.

## Tests - `tests/test_agent.py` (no network, no real LLM)
- candidates: `select_candidates("당진", "mass_centered")` returns the 당진 sites first; `select_candidates("대전", "walking")` expands by radius and includes 대흥동 주교좌성당; result dicts have lat/lng floats.
- validators: build a valid PilgrimagePlan fixture (2 days, consecutive dates) -> no errors; then mutate: wrong day count, non-consecutive date, overlapping times, missing mass_time for mass_centered, budget exceeded -> each produces the expected error substring.
- strip_json_fences handles "```json\n{...}\n```", "```\n{...}\n```", and bare JSON.
- graph routing: monkeypatch the LLM factory where nodes import it (make sure nodes import `get_llm` in a patchable way, e.g. `from app.agent import llm as llm_module` and call `llm_module.get_llm`) with a fake chat model. Implement a small fake `BaseChatModel` subclass in the test with `bind_tools` returning self and scripted responses in order: research -> an AIMessage with a single tool_call to `get_weather` (args lat/lng/date); then AIMessage "조사 요약: ..."; compose -> first an INVALID JSON draft (e.g. missing `days`), then a VALID plan JSON. Monkeypatch `fetch_weather` / `fetch_route` where nodes/tools import them to return canned dicts (the tool objects call the module-level functions, so patch `app.tools.weather.fetch_weather` and `app.tools.route.fetch_route`). Assert: run_plan returns a plan that validates; emitted events include status stages collect/research/compose/finalize, a log with kind tool_call for get_weather, a decision log mentioning 검증 실패, and exactly one result event.
- run all: `.venv/Scripts/python -m pytest -q` must pass (15 existing + new).

## Report
Write `docs/task3-agent.md` in Korean: graph diagram (text), state fields, node responsibilities, persona rules, validation rules and retry policy, revise flow, event list, full `pytest -q` output. End your reply with a summary and any uncertainties.
