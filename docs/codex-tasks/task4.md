You are implementing Task 4 of the project in the current directory (Python 3.13 venv at `.venv`, FastAPI + LangGraph). Read README.md, docs/architecture.md (sections "API" and "SSE 이벤트 형식"), app/agent/graph.py, app/agent/events.py, app/agent/state.py, app/config.py and docs/task3-agent.md first. Do NOT modify specs/, docs/architecture.md, app/tools/, app/agent/, app/schemas/, data/, public/, or .env. Do not commit. Assume NO network: tests must not call the real LLM or APIs. All existing tests must keep passing.

## Goal of Task 4
Expose the agent as a FastAPI service with Server-Sent Events for progress/log streaming, an in-memory plan store, and a revise endpoint. Serve the static frontend from `public/`.

## Files to create
- `main.py` (repo root) - FastAPI app factory `create_app()` and module-level `app = create_app()`. CORS from `settings.ALLOWED_ORIGINS` (allow all if the list is empty). Mount `public/` at `/` with `StaticFiles(html=True)` LAST so API routes win. Include the router from `app/routers/plans.py`.
- `app/store.py` - `PlanStore` (in-memory, thread-safe with a `threading.Lock`): `create(request: dict) -> str plan_id (uuid4 hex[:12])`; records `{"id", "request", "status": "pending"|"running"|"done"|"error", "plan": dict|None, "research_notes": str, "error": str|None, "events": list[dict], "created_at"}`. `get(plan_id)`, `append_event(plan_id, event)`, `set_result(plan_id, plan, research_notes)`, `set_error(plan_id, message)`, `set_status(...)`. Module-level singleton `store = PlanStore()`.
- `app/services/planner.py` - `start_plan_job(plan_id)` and `start_revise_job(plan_id, revise_request)`: run `run_plan` / `run_revise` in a background thread (`threading.Thread(daemon=True)`), with an `emit` callback that (a) appends the event to the store and (b) pushes it onto an `asyncio.Queue` for every live subscriber of that plan_id (use `loop.call_soon_threadsafe`). Keep a `subscribers: dict[plan_id, set[asyncio.Queue]]` with `subscribe(plan_id)`/`unsubscribe`. When the job finishes, emit a final sentinel so streams close. On PlanGenerationError or any exception: `store.set_error` and emit `{"event":"error","data":{"message": ...}}`. A revise job stores the new plan over the old one and keeps the previous plan in `record["history"]` (list).
- `app/routers/plans.py` - APIRouter prefix `/api`:
  - `GET /api/config` -> `{"naver_map_client_id": settings.NAVER_MAP_CLIENT_ID, "personas": [{"key", "label"}...] from app.agent.personas}` (Client ID is public; never return the secret).
  - `GET /api/sites?region=대전` -> candidate sites from `select_candidates` (for the map's initial view). Optional.
  - `POST /api/plans` body = PlanRequest fields (`region, start_date, nights, party_size, budget_per_person, persona, preferences`) -> validate with `PlanRequest` (422 on error), create store record, start job, return `{"plan_id": id, "stream_url": f"/api/plans/{id}/stream"}` with status 202.
  - `GET /api/plans/{id}/stream` -> `sse_starlette.EventSourceResponse`. First replay every event already in `record["events"]` (so a late subscriber sees history), then stream live events until the sentinel. Each SSE message: `event` = event name, `data` = `json.dumps(data, ensure_ascii=False)`. Send a `ping` comment every 15 s to keep the connection alive. 404 if unknown id.
  - `GET /api/plans/{id}` -> `{"id", "status", "request", "plan", "error"}`; 404 if unknown.
  - `POST /api/plans/{id}/revise` body `{"instruction": str}` -> 404 unknown, 409 if the plan is not `done`, else set status running, clear `events` (keep history), start revise job, return 202 `{"plan_id", "stream_url"}`.
- `requirements.txt`: already contains fastapi, uvicorn, sse-starlette, httpx - do not add packages.

## Tests - `tests/test_api.py` (no network)
Use `fastapi.testclient.TestClient`. Monkeypatch `app.services.planner.run_plan` and `run_revise` with fakes that call `emit` with a status event, a tool_call log, and a result event (a valid plan dict; reuse the GOOD sample from tests/test_plan_schema.py) and return `{"plan": ..., "research_notes": "조사 요약: 테스트"}`. Cover: `GET /api/config` shape (no secret key in body); `POST /api/plans` 202 + 422 on bad persona; `GET /api/plans/{id}` transitions to done after the fake job (poll with a short loop / join the thread via a hook the test can await - e.g. expose `planner._threads[plan_id]` so the test can `.join()`); `GET /api/plans/{id}/stream` - use `with client.stream("GET", url) as r` and read lines until the result event; assert `event: status`, `event: log`, `event: result` appear in order and the data is valid JSON; `POST /api/plans/{id}/revise` -> 409 while running, 202 when done, and the new plan replaces the old one with the previous kept in history; 404s.
Run `.venv/Scripts/python -m pytest -q` - everything must pass.

## Report
Write `docs/task4-api.md` in Korean: endpoint table with request/response examples, SSE message example (raw text), store lifecycle, threading/queue design, and the full `pytest -q` output. Also add a `## API` section to README.md (endpoint table only; keep the rest of README intact). End your reply with a summary and any uncertainties.
